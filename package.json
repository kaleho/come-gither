{
	"name": "come-gither",
	"version": "0.3.1",
	"description": "Obsidian plugin: sync a vault with a GitHub repository through the GitHub API",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"test": "vitest run",
		"coverage": "vitest run --coverage"
	},
	"keywords": [],
	"author": "Ray Kaleho",
	"license": "MIT",
	"dependencies": {
		"node-diff3": "^3.1.2"
	},
	"devDependencies": {
		"@types/node": "^20.11.0",
		"@vitest/coverage-v8": "^2.1.9",
		"esbuild": "^0.24.2",
		"obsidian": "^1.7.2",
		"typescript": "^5.7.3",
		"vitest": "^2.1.9"
	}
}
